<%= form_for(@ventum) do |f| %>
  <% if @ventum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ventum.errors.count, "error") %> prohibited this ventum from being saved:</h2>

      <ul>
      <% @ventum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fecha %><br>
    <%= f.date_select :fecha %>
  </div>
  <div class="field">
    <%= f.label :cantidad_productos %><br>
    <%= f.number_field :cantidad_productos %>
  </div>
  <div class="field">
    <%= f.label :total %><br>
    <%= f.number_field :total %>
  </div>

  <div class="field">
    <%= f.label :empleado_id %><br>
    <%= f.select :empleado_id, Empleado.all.collect {|p| [ p.nombre, p.id ] }, prompt: true  %>
  </div>
  <div class="field">
    <%= f.label :cliente_id %><br>
     <%= f.select :cliente_id, Cliente.all.collect {|p| [ p.nombre, p.id ] }, prompt: true  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

    