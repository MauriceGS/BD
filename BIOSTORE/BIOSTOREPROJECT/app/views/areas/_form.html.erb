<%= form_for(@area) do |f| %>
  <% if @area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@area.errors.count, "error") %> prohibited this area from being saved:</h2>

      <ul>
      <% @area.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="field">
    <%= f.label :total_empleados %><br>
    <%= f.number_field :total_empleados %>
  </div>
  <div class="field">
    <%= f.label :proveedor_id %><br>
    <%= f.select :proveedor_id, Proveedor.all.collect {|p| [ p.nombre, p.id ] }, prompt: true  %>
    
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
